# Runtime dependencies (mirror of [project.dependencies] in pyproject.toml).
# Use `pip install -r requirements.txt` for environments that don't install
# the package itself (e.g. plain `python -m uvicorn ...`).

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0
structlog>=24.1
sqlalchemy>=2.0
psycopg[binary]>=3.2
pgvector>=0.3
boto3>=1.34
botocore>=1.34
python-pptx>=1.0
Pillow>=10.3
langgraph>=0.2.50
pybreaker>=1.2
openai>=1.40
simhash>=2.1
docling>=2.14
langchain-text-splitters>=0.3.4

# Dev/test extras (install with: pip install -r requirements.txt[dev]
# is not supported; use `pip install -e .[dev]` instead).
pytest>=8.2
pytest-mock>=3.14
httpx>=0.27
hypothesis>=6.100
